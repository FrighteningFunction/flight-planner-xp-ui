# Flight Planner UI

The Graphics Interface of the Flight Planner web application. You may run the app in the following way:

First clone the repo, then using npm, run

```bash
npm install
npm run build
npm start
```

Note: you need to start the backend of the app as well, in order to use it.
